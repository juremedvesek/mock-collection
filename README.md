# Safe use of secrets

Secrets should be collected to common place, and also be encrypted by Ansible vault.

Here we show a toy example of that.

In real world one should also prefix variables by role name, to avoid duplicates.
